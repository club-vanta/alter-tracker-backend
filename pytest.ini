[pytest]
testpaths = tests
asyncio_mode = auto
pythonpath = .
# Show short test summary for failures and errors
addopts = -ra -q
# Fail fast on first error during collection
filterwarnings =
    ignore::DeprecationWarning
